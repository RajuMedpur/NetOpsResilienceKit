apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: busybox-pod-kill
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - chaos-testing
    labelSelectors:
      app: busybox
  duration: "30s"
  scheduler:
    cron: "@every 2m"
